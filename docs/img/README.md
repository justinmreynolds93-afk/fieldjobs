# docs/img/

Screenshots for the main README. All captured against the seeded demo data
(`SeedData.cs`) — "Sample Client A/B", `123 Example St`, `$100` placeholder
amounts. No real client, address, or rate information.

| file | shows |
|---|---|
| `dashboard.png` | the dashboard: waiting-on split, receivables, needs-attention feed |
| `jobs-list.png` | the jobs grid — config-driven `Ref #` column |
| `job-detail-stages.png` | a job's gated-stage checklist |

To re-capture: run the installed app (`dist/FieldJobs-Setup-*.exe` or
`dotnet run --project src/FieldJobs`), `Win`+`Shift`+`S` each window, paste.
