<UserControl x:Class="FieldJobs.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="28,24">
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Payments" Style="{StaticResource H1}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="+ Record payment" Style="{StaticResource Primary}"
                    VerticalAlignment="Center" Click="Record_Click" />
        </Grid>

        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Show:" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox x:Name="PeriodBox" Width="200" SelectionChanged="Period_Changed" />
            <TextBox x:Name="SearchBox" Width="280" Margin="12,0,0,0" TextChanged="Search_Changed"
                     ToolTip="Filter by job, invoice #, method or reference" />
            <TextBlock x:Name="TotalsText" HorizontalAlignment="Right" VerticalAlignment="Center"
                       FontWeight="SemiBold" />
        </DockPanel>

        <Border Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" MouseDoubleClick="Grid_Open">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding PaymentDate}" Width="110" />
                    <DataGridTextColumn Header="Job" Binding="{Binding JobTitle}" Width="*" />
                    <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="110" />
                    <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="150" />
                    <DataGridTextColumn Header="Reference" Binding="{Binding Reference}" Width="150" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="110" />
                    <DataGridTextColumn Header="Note" Binding="{Binding Note}" Width="180" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
