<UserControl x:Class="FieldJobs.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="28,24">
        <TextBlock DockPanel.Dock="Top" Text="Invoices" Style="{StaticResource H1}" />

        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Search / filter:" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox x:Name="SearchBox" Width="360" HorizontalAlignment="Left" TextChanged="Search_Changed"
                     ToolTip="Match job, invoice number, stage, or status" />
            <TextBlock x:Name="TotalsText" DockPanel.Dock="Right" HorizontalAlignment="Right"
                       VerticalAlignment="Center" FontWeight="SemiBold" />
        </DockPanel>

        <Border Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" MouseDoubleClick="Grid_Open">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="110" />
                    <DataGridTextColumn Header="Job" Binding="{Binding JobTitle}" Width="*" />
                    <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate}" Width="110" />
                    <DataGridTextColumn Header="Stage / section" Binding="{Binding StageLabel}" Width="180" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
                    <DataGridTextColumn Header="Billed" Binding="{Binding Billed, StringFormat=C}" Width="100" />
                    <DataGridTextColumn Header="Paid" Binding="{Binding Paid, StringFormat=C}" Width="100" />
                    <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat=C}" Width="100" />
                    <DataGridTextColumn Header="Last payment" Binding="{Binding PaymentDate}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
