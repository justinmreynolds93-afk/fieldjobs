<UserControl x:Class="FieldJobs.Views.JobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Jobs" Style="{StaticResource H1}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="+ New job" Style="{StaticResource Primary}"
                    VerticalAlignment="Center" Click="NewJob_Click" />
        </Grid>

        <DockPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Style="{StaticResource Muted}"
                       Text="Search:" Margin="0,0,8,0" />
            <TextBox x:Name="SearchBox" Width="380" HorizontalAlignment="Left"
                     TextChanged="SearchBox_TextChanged"
                     ToolTip="Search address, client, job number, Ref number, or status" />
            <TextBlock x:Name="CountText" DockPanel.Dock="Right" HorizontalAlignment="Right"
                       VerticalAlignment="Center" Style="{StaticResource Muted}" />
        </DockPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" MouseDoubleClick="Grid_Open">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="2*" />
                    <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="*" />
                    <DataGridTextColumn Header="Job #" Binding="{Binding JobNumber}" Width="90" />
                    <DataGridTextColumn Header="Ref #" Binding="{Binding ExternalRef1}" Width="90" />
                    <DataGridTextColumn Header="Type" Binding="{Binding ProjectType}" Width="120" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130" />
                    <DataGridTextColumn Header="Current stage" Binding="{Binding CurrentStage}" Width="160" />
                    <DataGridTemplateColumn Header="Progress" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <ProgressBar Value="{Binding PercentComplete}" Maximum="100" />
                                    <TextBlock Text="{Binding PercentComplete, StringFormat={}{0}%}"
                                               Style="{StaticResource Muted}" FontSize="10" HorizontalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Outstanding" Binding="{Binding Outstanding, StringFormat=C}" Width="110" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
